from django.apps import AppConfig

class MicroInternshipsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.micro_internships'
    verbose_name = 'Micro Internships'
